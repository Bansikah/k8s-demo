## Commands used

```plain text
docker network rm coffeetime_default
docker network create --subnet=10.100.0.0/16 coffeetime_coffee
docker-compose up -d 
 docker-compose down
docker-compose ps
docker network ls
docker inspect coffeetime_coffee  

sudo apt install openssh-server


```